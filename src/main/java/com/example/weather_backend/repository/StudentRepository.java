package com.example.weather_backend.repository;

import org.springframework.stereotype.Repository;

@Repository
public class StudentRepository {

    // assume it as database call
    public int getId() {
        return 1;
    }
}
